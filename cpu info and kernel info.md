    #include <iostream>
    #include <stdlib.h>
    #include <stdio.h>
    
    using namespace std;
    
    int main() {
      system ("cat /proc/cpuinfo");
    }
to run this code save as .cpp and use the below command:

        g++ cpu_info.cpp
        
followed by:

        ./a.out
